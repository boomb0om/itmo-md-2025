# itrmo-md-2025 airflow

## Настройка и запуск airflow

Настройте переменные окружения в `.env` файле. Пример есть в `.env.example`.

Запустите airflow:

```bash
docker compose -f docker-compose-airflow.yaml up
```

Перейдите на http://localhost:8080 и залогиньтесь